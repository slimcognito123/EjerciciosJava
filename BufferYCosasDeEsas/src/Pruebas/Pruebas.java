package Pruebas;

import java.io.*;

public class Pruebas {
    public static void main(String[] args) throws IOException {

       /* InputStreamReader algo = new InputStreamReader(System.in);
        BufferedReader teclado = new BufferedReader(algo);
        String linea = teclado.readLine();
        System.out.println(linea);*/

      /* FileOutputStream ficheroSalida = new FileOutputStream("precios.cat",true);
        DataOutputStream salida=new DataOutputStream(ficheroSalida);
        salida.writeDouble(234567890.43567890);
        salida.close();

        FileInputStream ficheroEntrada = new FileInputStream("precios.cat");
        DataInputStream entrada = new DataInputStream(ficheroEntrada);
        double precio= entrada .readDouble();
        System.err.print(precio);
        entrada.close();*/


    }
}
